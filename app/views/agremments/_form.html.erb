<%= form_with(model: agremment) do |form| %>
  <% if agremment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agremment.errors.count, "error") %> prohibited this agremment from being saved:</h2>

      <ul>
        <% agremment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_area :state %>
  </div>

  <div class="field">
    <%= form.label :users_id %>
    <%= form.text_field :users_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
