<%= form_with model: [ @store, @product, @comment ], local: false do |form| %>
  <div class="container">
    <%= form.text_area :body, class: "comment_body form-control", cols: 50, style: "resize: none; height: 100px;"%>
    <%= form.submit class: "btn btn-primary ", id: "create_comment"%>
  </div>
<% end %>
